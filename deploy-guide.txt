# 1. Verify everything
python setup_check.py

# 2. Build containers
docker-compose build

# 3. Start system
docker-compose up

# 4. Monitor logs in different terminals
docker logs -f evcharging_central
docker logs -f evcharging_cp_engine_1
docker logs -f evcharging_driver_1

# Connect to driver_2 interactively
docker attach evcharging_driver_2

🔴 Problem: docker logs -f is read-only, can't send input
✅ Solution: Use docker attach evcharging_driver_2 for interactive menu